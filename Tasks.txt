X Take and have items
- Require an item for an exit
X Ability to list objects
- Abbreviate direction commands
- Update everything to use string as entity reference rather than index or id
- Remove the need to specify the location of things twice
- Implement Game.Do(string)
- Cleanup build dir, where is binary?